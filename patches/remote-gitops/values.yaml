---
remoteGitOps:
  applications:
    - name: b
      project: tower-ops
      destination:
        name: tower
        namespace: argo
      sources:
        - repoURL: https://github.com/SmartX-Team/smartx-k8s.git
          targetRevision: scalex-sandbox-smartx-k8s
          path: .
          helm:
            releaseName: smartx
            valueFiles:
              - $cluster/values.yaml
            valuesObject:
              applications:
                root:
                  enabled: false
        - repoURL: https://github.com/SmartX-Team/b-k8s.git
          targetRevision: main
          ref: cluster
      syncPolicy:
        automated:
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - RespectIgnoreDifferences=true
          - ServerSideApply=true
    - name: c
      project: tower-ops
      destination:
        name: tower
        namespace: argo
      sources:
        - repoURL: https://github.com/SmartX-Team/smartx-k8s.git
          targetRevision: scalex-sandbox-smartx-k8s
          path: .
          helm:
            releaseName: smartx
            valueFiles:
              - $cluster/values.yaml
            valuesObject:
              applications:
                root:
                  enabled: false
        - repoURL: https://github.com/SmartX-Team/c-k8s.git
          targetRevision: main
          ref: cluster
      syncPolicy:
        automated:
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - RespectIgnoreDifferences=true
          - ServerSideApply=true
    - name: tower-scalex-federation
      annotations:
        argocd.argoproj.io/sync-wave: "40"
      project: tower-ops
      destination:
        name: tower
        namespace: argo
      source:
        repoURL: https://github.com/SmartX-Team/scalex-sandbox-federation.git
        targetRevision: main
        path: .
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - RespectIgnoreDifferences=true
          - ServerSideApply=true
