---
# Expose the Tower Argo CD UI through the host cluster load balancer.
server:
  service:
    type: LoadBalancer

# Enable the built-in admin account for the lab-only Tower Argo UI.
configs:
  cm:
    admin.enabled: true

